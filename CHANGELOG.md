# Changelog

## v1.1.6

- 介面繁體中文化：點歌網頁、OBS 浮層，以及 mod 回傳給網頁的訊息（點歌結果、分享狀態等）全部改為台灣正體用語（例：搜尋、載入、預設、連結、遠端分享）
- 網頁字型優先使用正體中文字型（微軟正黑體 / PingFang TC / Noto Sans TC）
- `SongRequestMod.toml` 的設定鍵名維持原樣（例如「网页端口」），舊設定檔照常可用

## v1.1.5

- 新增：**定数**（谱面常数，如 13.7）。难度块直接显示定数，鼠标悬停显示「等级 · 定数」；正在游玩面板也带定数；`/api/songs` 每个难度新增 `const`、每首新增 `maxConst`
- 新增：「等级 / 定数」筛选两种写法通用，可混用：`12`、`13+`（等级）或 `12.0`、`13.4`（定数）。`13` = 13.0~13.6、`13+` = 13.7~13.9（分界按曲库数据自动判断）；上下限填反会自动对调；认不出的输入标红。选了难度时只看那一档，没选时任一可玩难度落在区间内即算
- 改进：「等级 ↑↓」排序改为按定数排序
- 新增：搜索简体 / 繁体 / 日文字形通用（例：樱 / 櫻 / 桜、爱 / 愛、头 / 頭 互通），对曲名、艺术家、别名都有效（对照表来自 OpenCC，见 NOTICE）
- 改进：`崩溃保护` 改为默认关闭，可在 toml 里 `崩溃保护=true` 打开（同原作者 v1.0.4：它会吞掉游戏异常，可能出现「歌全没了」等异常表现；登录闪退的根本修复是「游戏数据就绪后才工作」）
- 内部：选曲子序列改用 `AccessTools.FieldRefAccess` / `MethodDelegate` 的有类型访问，不再反射取值（字段不对会在进选曲界面时明确报错）

## v1.1.4

- 网页：修复「难度」筛选按钮在电脑上文字被截断成「B…」「M…」的问题（难度栏占两格，标签统一缩写为 BAS / ADV / EXP / MAS / Re:M）

## v1.1.3

稳定性修复（点歌时游戏卡死 / 无响应）：

- 修复：实时面板推送（SSE）由游戏主线程直接写网络，只要有一个浏览器不收数据（手机锁屏 / 切后台 / 网络卡 / 隧道流控），主线程就会永久阻塞，游戏「没有回应」。现在每个连接一条写线程，主线程只放最新状态；写超过 10 秒没完成的连接自动断开
- 修复：曲目表索引、别名表是普通 Dictionary，主线程和网页线程会同时清空重写，可能把表写坏导致查询死循环（游戏卡死）。现在只在主线程重建，建好整张新表再替换引用；网页线程只读缓存，需要游戏数据的请求（刷新曲库 / 自检）交给主线程执行
- 修复：曲库 rev 每重建一次就 +1，网页看到 rev 变化又强制重建，开着两个以上页面时互相触发，每几秒整表重建并清空封面缓存。现在内容真的变了才 +1，自动同步不再要求重建，只有手动「刷新曲库」才重建
- 修复：点歌不检查游戏状态、连点时同一帧把所有请求都执行一遍。现在：只在选曲主流程且处于选曲列表/难度画面时跳转（其它画面提示先回选曲列表）；游戏锁输入（切画面动画中）时先等最多 3 秒；连点只执行最新一个，前面的回「被取代」（网页不报错）；切难度画面改用游戏自己的 SyncNext（锁输入 1.2 秒、所有玩家一起切、宴会场曲目进宴会场难度画面）；已在难度画面时先正确退回选曲列表，修复之后按返回回不去的问题；不会跳进对战/挑战/段位/联机等特殊分类
- 修复：封面请求在网页线程里同步等主线程编码（最多 6 秒），4 个网页线程被封面占满时点歌只能排队。现在封面异步返回，回写用专用后台线程
- 优化：封面编码每帧最多 4ms、游玩中每 10 帧最多一张；列表小图缩到 160px；缓存按大小淘汰旧图，重进选曲界面不再清空；排队超过 15 秒的封面请求直接放弃
- 优化：不在游玩中时每秒 20 次 FindObjectOfType 改为每秒最多 1 次；游玩中不再每 2 秒扫描曲库；别名文件每次导出只检查一次（以前每首歌查一次）
- 修复：局域网 IP 绑定失败时整个点歌台起不来，现在退回只开本机
- 修复：网页线程调用 Unity API（Application.dataPath / FindObjectOfType / Time）、页面缓存并发写
- 网页：游戏数据加载中时自动重试；SSE 连着时轮询从 180ms 降到 2 秒一次，SSE 断了 5 秒后自动重连

## v1.1.2

- 修复：装 mod 后登录闪退（`PlInformationProcess.RestoreGhost` NullReference）。移植原作者 v1.0.3 的两处修复：
  - 游戏数据（`DataManager`）加载完之前 mod 什么都不做，日志出现「游戏数据就绪, 开始工作」后才开始读曲目表 / 游玩状态
  - 崩溃保护 `CrashGuard`：段位 BOSS 找不到时退到最近的 BOSS；曲目分类不存在时顶成存在的分类；`RestoreGhost` 与选曲分类页签的异常直接吞掉，不让游戏闪退
- 远程分享改为游戏数据就绪后才启动，不在游戏加载期间起外部进程
- 启动点歌台前先清掉上次崩溃残留的隧道进程（修复重开游戏后「端口 8790 被占用」）

## v1.1.1

- 修复：首次下载 cloudflared 时会修改游戏进程全局的 HTTPS 设置（`ServicePointManager.SecurityProtocol`），可能连带影响游戏自己的联网；现在改由 curl.exe / PowerShell 外部进程下载，不碰游戏进程
- 修复：点歌台启动失败（端口被占）时仍会自动开启远程分享，隧道会指向占用该端口的其他程序；现在点歌台没起来就不开分享，日志提示常见原因（上次游戏没关干净）
- 修复：内嵌的 `System.Diagnostics.Process` 与游戏的 `Process` 命名空间同名导致用真实游戏 dll 编译失败

## v1.1.0

- 新增：**远程分享**。游戏启动后自动用 Cloudflare 免费临时隧道生成一个公网链接（`https://xxxx.trycloudflare.com/?k=密钥`），发给别人就能远程搜歌、点歌、看实时面板；免注册、免改路由器 / 防火墙。首次使用自动下载 `cloudflared.exe` 到 `Mods\SongRequestMod\`
- 远程访问必须带本次分享的随机密钥，每次开启都换新链接；远程访问者不能开关分享、不能用诊断接口
- 本机网页新增「分享链接」面板：复制链接 / 关闭分享 / 重新开启；分享已开启时本机打开网页会自动弹出
- 隧道等真正连上 Cloudflare 才给出链接（不会给出打不开的链接）；UDP 不通时自动改用 TCP 重试；退出游戏自动关闭，崩溃残留的进程下次启动时清理
- 新增配置 `远程分享自动开启`（默认 true）
- 升级提示：如果 `Mods\SongRequestMod\page.html` 放着旧版页面，mod 会优先用它，看不到「分享链接」按钮 —— 删掉它（用 dll 内嵌的新页面）或换成新版
- 修复：配置文件补全缺项时会把用户改过的值（端口、局域网访问等）全部重置成默认值；现在按当前值重写

## v1.0.2

- 修复：某些版本/数据下曲库显示 0 首。原因是 `DataManager` 的内部曲目表在加载完成前是**非 null 的空占位表**（`CreateDummyTable<T>()`），旧逻辑只在字段为 null 时才回退公开 API，救命路径从未被尝试；且游戏自己的 `LoadData` 会在装载阶段用 `IsDisable()` 把条目先剔掉，被剔干净后表就永远是空的。现在改为**多路探测**（选曲列表主路 + 公开 API + 反射全部候选字段 + 形状自适应抽取 + 逐条 try/catch），取条目最多的那一路；离开选曲界面后 `CombineMusicDataList` 会被游戏释放，改用最后一次成功的快照兜住，曲库不会因此掉回 0 首
- 修复：`disable` 标记在个别数据下把整库过滤为空；新增"整库被清空就自动关闭过滤"的保险，并优先用 `MusicData.disable` 字段值而不是仅看 `IsDisable()`
- 修复：`isExistsScore` 在 1.70 实际是 `List<bool>`，旧代码按 `bool[]` 读（`as bool[]`）永远是 null，导致"这档谱到底能不能玩"的游戏真值一直静默失效；现在 `bool[]` / `List<bool>` / `IList` 都认，不可玩的难度会正确置灰
- 修复：曲目表为空时会反复重建（rev 暴涨、CPU 白耗、曲绘缓存被反复清空）；现在改为节流重试（5 秒一次、最多 3 次后放弃）
- 修复：`/api/status` 的别名计数在曲库为空时永远显示 0（改为读取时即解析）
- 新增：`/api/selfcheck` 里输出曲目表数据源诊断（用了哪一路、各拿到多少条、形状、异常、disable 过滤状态），便于排查

## v1.0.1

- 曲目别名库更新：**1404 首 / 10592 条**（数据来自 MuNET 别名数据库）
- **配置文件自动补全**：启动时发现旧配置缺项（例如新增的「局域网访问」）会自动补上并重写 `SongRequestMod.toml`，保留用户已改过的值
- 启动日志去掉重复的地址行，只打一次
- 手机端顶部区域压缩（不再占掉大量屏高）、Header 不再换行堆高

## v1.0.0

- 首个公开版本
